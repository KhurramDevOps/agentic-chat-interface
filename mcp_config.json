{
  "mcpServers": {
    "tavily-search": {
      "command": "npx",
      "args": ["-y", "@tavily/mcp"],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}"
      },
      "description": "Tavily Search MCP server — provides tavily_search and tavily_extract tools",
      "agent": "ResearchAgent"
    }
  }
}
