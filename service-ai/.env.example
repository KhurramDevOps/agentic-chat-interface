# ─────────────────────────────────────────────
# service-ai — Environment Configuration
# Copy to .env and fill in real values.
# NEVER commit .env to source control.
# ─────────────────────────────────────────────

# Runtime
APP_ENV=development          # development | staging | production
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# LiteLLM / Gemini routing
LITELLM_MODEL=gemini/gemini-1.5-pro   # OpenAI-style alias mapped by LiteLLM
GEMINI_API_KEY=gemini-api-key-here

# mem0 local agentic memory
MEM0_API_KEY=your-mem0-api-key-here   # leave blank to use local in-memory mode

# OpenAI Agents SDK base URL (points to LiteLLM proxy if self-hosted)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=                       # optional: override for LiteLLM proxy

# Background task limits
MEDIA_TASK_TIMEOUT_SECONDS=300
MEDIA_TASK_MAX_QUEUE=50
